# Pinned runtime dependencies for the released Python engine.
# Install with: python -m pip install -r requirements.lock.txt
# Do not add yara-python here. YARA remains an optional extra.

volatility3==2.28.0
pefile==2024.8.26
